<Window x:Class="PapaPersonas.App.Processes.SergioUnknownHeadersDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Columnas desconocidas"
        SizeToContent="WidthAndHeight"
        MinWidth="560"
        MaxWidth="760"
        WindowStartupLocation="CenterOwner"
        ResizeMode="CanResize">
    <Grid Margin="18">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="220" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Margin="0,0,0,12" TextWrapping="Wrap" Text="El archivo contiene columnas desconocidas. Se ignorarán y no se guardarán en la base, el área de preparación ni los metadatos de importación." />
        <ListBox x:Name="UnknownHeadersList" Grid.Row="1" />
        <StackPanel Grid.Row="2" Margin="0,16,0,0" HorizontalAlignment="Right" Orientation="Horizontal">
            <Button Width="110" Margin="0,0,10,0" Content="Cancelar" IsCancel="True" Click="Cancel_Click" />
            <Button Width="110" Content="Continuar" IsDefault="True" Click="Continue_Click" />
        </StackPanel>
    </Grid>
</Window>
